#include <iostream>

using namespace std;
using tp = long long int;

signed main() {
    tp n, cnt = 0;
    cin >> n;
    for (string s; n--; cnt += s == "Snigellin") {
        cin >> s;
    }
    cout << cnt << '\n';
    return 0;
}